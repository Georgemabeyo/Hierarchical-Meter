services:
  - type: web
    name: water-dashboard
    env: static
    branch: main   # badilisha kwa branch yako ya GitHub ikiwa ni tofauti
    plan: free
    buildCommand: ""   # kwa static site hatuhitaji build command
    startCommand: ""   # kwa static site hatuhitaji start command
    autoDeploy: true   # inafanya deploy kila push kwenye branch hii
    envVars: []        # unaweza kuongeza environment variables kama utahitaji
